<div id="item-edit-form">
  <table class="table table-hover">
    <thead>
      <th>Date</th>
      <th>Total</th>
      <th>Status</th>
    </thead>
    <tbody>
      <% @orders.each do |order| %>
      <tr class="order order_<%= order.id %>">
        <td class="date"><%= order.updated_at %></td>
        <td class="total">$<%= order.total %></td>
        <td class="status"><%= order.status %></td>
      </tr>
      <% end %>
    </tbody>
  </table>
</div>
