<div class="sidenav">
    <ul class="nav nav-pills nav-stacked sidenav" id="sidebar">
      <% @categories.each do |category| %>
        <li class="menu-link"><%= link_to category.name, "#category-id-#{category.id}" %></li>
      <% end %>
    </ul>
</div>
