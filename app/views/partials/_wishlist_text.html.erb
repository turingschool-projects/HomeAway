<% if @fave %>
	<div class="col-sm-2 wishlist-text" data-id="<%= @property.id %>">Remove from Wishlist</div>
<% else %>
	<div class="col-sm-2 wishlist-text" data-id="<%= @property.id %>">Add to Wishlist</div>
<% end %>